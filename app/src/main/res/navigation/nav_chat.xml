<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_chat"
    app:startDestination="@id/chat">

    <fragment
        android:id="@+id/chat"
        android:name="com.sigmai.stylemento.feature.chat.ChatFragment"
        android:label="chat"
        tools:layout="@layout/fragment_chat">
        <action
            android:id="@+id/action_chat_to_chat_room"
            app:destination="@id/chat_room" />
    </fragment>

    <fragment
        android:id="@+id/chat_room"
        android:name="com.sigmai.stylemento.feature.chat.chat_room.CharRoomFragment"
        android:label="chat_room"
        tools:layout="@layout/fragment_chat_room"/>
</navigation>